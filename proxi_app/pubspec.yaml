name: proxi_app
description: "Proxi — ubicación precisa en vivo (MVP con UWB)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  torch_light: ^1.1.0
  screen_brightness: ^2.1.3
  flutter_blue_plus: ^1.32.0
  flutter_ble_peripheral: ^1.2.6
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
